/// ========================================================
/// OBJ_FOG
/// CLEAN UP
/// ========================================================


// ========================================================
// EMISSOR
// ========================================================

if (fog_sys != -1)
{
    part_emitter_destroy(
        fog_sys,
        fog_emit
    );
}


// ========================================================
// TIPO
// ========================================================

if (fog_type != -1)
{
    part_type_destroy(
        fog_type
    );
}


// ========================================================
// SISTEMA
// ========================================================

if (fog_sys != -1)
{
    part_system_destroy(
        fog_sys
    );
}


// ========================================================
// SURFACE
// ========================================================

if (surface_exists(surf_fog))
{
    surface_free(
        surf_fog
    );
}

surf_fog = -1;
/// ========================================================
/// scr_particle_emit_burst
/// FASE 4.4
/// ========================================================

function particle_emit_burst(
    _x,
    _y,
    _type,
    _amount
)
{
    particle_emit(
        _x,
        _y,
        _type,
        _amount
    );
}